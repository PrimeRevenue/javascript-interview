/*
 * @wordWrap
 * Write a word wrap function.
 *
 * 1. Insert new-line characters in the places where the line should wrap to avoid exceeding a max line length.
 *
 * 2. Remove all white spaces at the start of a line.
 *
 * 3. Review index.spec.ts to understand the expected outputs
 *
 * Run 'yarn test' to validate your logic.
 */

export const wordWrap = (text: string, characterLimit: number): string => {
    // Put your code here

    return ''; // this is a placeholder for your return type
};
